[Debug]
profile = true
device_counters = true
# device_trace left OFF: fine trace floods the HBM[31] buffer and its traffic
# contends with real HBM reads, inflating CU time (717 ms vs ~470 ms). The AIM
# per-port counters below come from device_counters and are clean without it.
